# Block-Sparse-Attention-Aware KV cache

This is the repo for CMU 15442 project (26Spring).

We propose a block-sparse-attention-aware KV cache eviction algorithm for higher hit rates compared to conventional approaches by simulation.
